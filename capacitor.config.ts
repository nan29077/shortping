import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.shortping.app',
  appName: '숏핑',
  webDir: 'dist',
  backgroundColor: '#111519',
};
export default config;
