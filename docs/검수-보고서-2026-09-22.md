# 숏핑 코드 검수 보고서 (2026-09-22)

검수 범위: `server/` 전체(API·인증·DB·핑/라마/정산·AI 엔진), `src/` 전체(시청자·PD 스튜디오·관리자 화면), `tests/`.
검수 방법: 코드 전량 정독 + 서버 zod 스키마와 화면 호출 대조 + 자동 검사(타입 검사, 테스트 50건, 빌드).

## 결과 요약

| 항목 | 결과 |
| --- | --- |
| 타입 검사 (`tsc --noEmit`) | 통과 |
| 테스트 (`npm test`) | **57/57 통과** (3차 작업 후, 신규 7건 포함) |
| 빌드 (`vite build`) | 통과 |
| 이번에 바로 고친 버그 | 서버 8건, 화면 20건 |
| 남겨 둔 개선 권고 | 12건 → 2차 작업에서 모두 반영 (2절) |

전체적으로 결제·정산·권한 처리는 트랜잭션과 멱등키, 소유권 검사가 잘 갖춰져 있고 테스트 커버리지도 높습니다. 발견된 문제는 대부분 경계 조건과 PostgreSQL 전환 시 드러날 타입 차이, AI 엔진의 동시성 문제였습니다.

## 1. 이번에 수정한 항목

### 서버

| 심각도 | 파일 | 문제 | 조치 |
| --- | --- | --- | --- |
| 높음 | `server/ai/engine.mjs` `kick()` | AI 작업 등록(`enqueue`)이 트랜잭션 안에서 `setTimeout`을 만들어, 이후 엔진의 모든 DB 작업이 그 트랜잭션 컨텍스트(AsyncLocalStorage)를 물려받음. SQLite에선 직렬화 큐를 우회하고, PostgreSQL에선 이미 반납된 커넥션으로 쿼리가 나가 작업이 이중 실행되거나 `running` 상태로 영구 정지될 수 있음 | `db.detach()`를 추가해 타이머를 트랜잭션 컨텍스트 밖에서 생성 |
| 높음 | `server/db.mjs` | PostgreSQL은 `COUNT/SUM`(bigint)과 `NUMERIC`을 **문자열**로 반환. 화면 곳곳(`누적 결제`, 연간 지급액 합계, 진행률 등)이 `"0"+"12000"` 문자열 연결·`NaN%`로 깨짐 | `pg.types.setTypeParser`로 숫자로 받도록 설정 (RDS 전환 전 필수) |
| 중간 | `server/ai/engine.mjs` `execute()` | 실패 처리(`fail`) 자체가 예외를 내면 작업이 `running`·`claimed_by` 상태로 남아 라마 예약이 풀리지 않음 | 최후 수단으로 작업을 `failed`로 닫는 안전망 추가 |
| 중간 | `server/ai/routes-studio-ai.mjs` 인물 삭제 | `cid` 소속 검사 전에 전 프로젝트 대상 `UPDATE studio_shots SET speaker_id=NULL` 실행 → 남의 프로젝트 컷 화자 초기화 가능 | 프로젝트 소속 확인 후 처리 |
| 중간 | `server/ai/routes-studio-ai.mjs` 결과 버전 적용 | 인물의 "목소리 미리듣기" 결과를 적용하면 `image` 컬럼에 mp3가 들어감 | kind별 컬럼 매핑(`image`/`voice_sample`) |
| 중간 | `server/ai/routes-studio-ai.mjs` 내보내기 | 직접 고른 포스터 `image`의 소유권 미검증 → 다른 PD 파일 사용 가능 | `media_files.owner_id` 검사 추가 |
| 중간 | `server/routes-admin.mjs` 월 마감 | 구독 매출 배분이 트랜잭션 없이 실행되어 중간 실패 시 절반만 기록될 수 있음 | `db.transaction`으로 묶음 |
| 낮음 | `server/ai/routes-studio-ai.mjs` `plan()` | `targetId` 누락 시 SQLite 바인딩 오류로 500 | 400으로 안내 |

### 화면(프론트엔드)

| 심각도 | 파일 | 문제 | 조치 |
| --- | --- | --- | --- |
| 중간 | `src/api.ts` + 5개 파일 | `crypto.randomUUID()`는 HTTPS/localhost에서만 존재. 폰에서 `http://192.168…`로 열면 결제·자동 열기·AI 실행이 TypeError | `uuid()` 헬퍼(폴백 포함)로 전부 교체 |
| 중간 | `src/api.ts` | 세션 만료(다른 기기 로그아웃·비밀번호 변경) 시 화면은 계속 로그인 상태로 보이고 모든 요청이 실패 | 401이면 전역 이벤트 → 로그아웃 처리 + 로그인 화면 이동 |
| 중간 | `src/api.ts` | 네트워크 단절 시 영어 오류(`Failed to fetch`) 노출 | 한국어 안내로 통일 |
| 중간 | `src/App.tsx` `reloadLibrary` | 일시적 오류에도 찜·지갑·시청기록을 통째로 비우고 재생 화면이 재마운트됨 | 로그인이 풀린 경우에만 초기화 |
| 중간 | `src/App.tsx` 부팅 | 라이브러리 로딩을 기다리지 않아 딥링크 진입 시 이어보기가 동작하지 않음 | `await reloadLibrary()` |
| 중간 | `src/App.tsx` 검색 | 검색어가 홈 피드에도 적용되어 홈이 비어 보임 / 대소문자 구분 | 검색·탐색 화면을 벗어나면 초기화, 소문자 비교 |
| 중간 | `src/App.tsx` 히어로 | 시드 작품 ID(`midnight/spring/moon`)·`12부작`·`첫 3화 무료`가 하드코딩 → 운영 DB에서 슬라이드가 죽고 숫자가 틀림 | 조회수 상위 3편 + 실제 회차 수/무료 회차 표시 |
| 중간 | `src/App.tsx` 주문 내역 | 라마 충전 주문이 `null 전체`로 표시 | `lama_charge` 분기 추가 |
| 중간 | `src/upload.ts` | 조각 업로드는 끝났는데 회차 등록만 실패한 뒤 "이어 올리기"하면 500MB를 처음부터 다시 올림 / 재확인 요청 실패 시 즉시 중단 / `1080p` 파일명을 80화로 인식 | `done` 세션은 바로 완료 호출, 재확인 실패 시 재시도 유지, 정규식 수정 |
| 중간 | `src/Settlement.tsx` | 구독 배분 항목이 마감 실행일 기준으로 달력에 잡혀 관리자 월별 집계와 어긋남 | `period` 기준으로 표시 |
| 중간 | `src/AdminSettlement.tsx` | 매월 31일에 `lastMonth()`가 이번 달을 돌려줘 마감 기본값 오류 | 1일 고정 계산 |
| 중간 | `src/Studio.tsx`, `StudioPanels.tsx`, `style.css` | 관리자가 "노출 중단"(`hidden`)한 작품이 PD 목록에서 상태 칩이 비고 어떤 필터에도 안 잡힘 | 라벨·필터·스타일 추가 |
| 중간 | `src/ChannelStudio.tsx` | 배너·문구 편집 중 카테고리 추가/삭제하면 편집 내용이 서버 값으로 덮여 사라짐 | 폼과 무관한 작업 뒤엔 폼 유지 |
| 중간 | `src/studio/Workspace.tsx` | AI 기획안이 인물 정보를 갱신해도 카드 폼이 옛 값을 유지해 "저장" 시 되돌림 | 손대지 않은 폼은 서버 값으로 재동기화 |
| 중간 | `src/EpisodeManager.tsx` | AI 도구 폴링이 모달을 닫아도 계속되고, 15분 후 조용히 종료 | 언마운트 시 중단, 종료 안내 |
| 낮음 | `src/AdminPoints.tsx` | 분배 비율 저장 후 입력창이 빈 칸·버튼 비활성 / 기준가 입력 지우면 0 나눗셈 `NaN원` | 편집값 삭제, 최소 1원 가드 |
| 낮음 | `src/api.ts` `count()`/`won()` | 문자열이 오면 천 단위 구분 없이 출력 | `Number()` 처리 |
| 낮음 | `src/StudioPanels.tsx` | 안내 문구 "최대 250MB"가 실제(500MB)와 다름 | 수정 |
| 낮음 | `src/studio/Workspace.tsx` | `costs.failed`가 `"0"`이어도 "실패 0(반환)" 표시 | 숫자 비교 |

## 2. 추가 권고 → 2차 작업에서 모두 반영 (2026-09-22)

1차 검수에서 "추가 권고"로 남겼던 12건을 모두 고쳤습니다. 수정 후 테스트 50/50, 타입 검사, 빌드 통과.

| # | 항목 | 조치 |
| --- | --- | --- |
| 1 | AI 작업 행 잠금 | 취소·완료·실패가 작업 행을 `FOR UPDATE`로 읽도록 변경(PostgreSQL). 라마 예약 이중 반환 방지. 실패 처리 안전망도 예약 라마를 먼저 돌려준 뒤 작업을 닫음 |
| 2 | 공급사 오류 원문 노출 | `ai_jobs.error_detail` 컬럼 추가. PD 화면에는 정리된 문구("AI 공급사에서 일시적인 문제가…")만, 원문은 관리자 AI 작업 목록에서만 표시. 키·주소 패턴은 가림 처리 |
| 3 | 장애 차단기 오탐 | 공급사 HTTP 오류(인증 실패·재시도 가능 오류)만 연속 실패로 집계. 형식 오류·파일 없음 등 내부 오류는 제외 |
| 4 | 금칙어 오탐 | 짧은 금칙어(한글 2자·영문 4자 이하)는 앞 글자가 같은 종류면 다른 단어의 일부로 보고 통과("칼로리", "denuded"). 긴 금칙어는 붙여 써도 차단("아이돌딥페이크") |
| 5 | 동시 요청 한도 초과 | AI 작업 등록 시 사용자 행 잠금 + 월 예산 설정 시 전역 advisory lock(PostgreSQL). 같은 멱등키 동시 요청은 500 대신 기존 작업 반환. 재시도 시 `started_at` 초기화 |
| 6 | 고아 파일 | 자막용 음성 파일은 작업 성공·실패·등록 실패 시 삭제. AI 결과 반영 실패·취소 시 결과 파일 삭제. 30일 지난, 어디에서도 참조하지 않는 업로드 파일을 매일 정리(참조 칸 11곳 확인) |
| 7 | 자막 파일·슬러그 경합 | 회차 삭제·자막 교체·자막 삭제 시 이전 `.vtt` 파일 삭제. 방송국 주소 동시 저장 시 500 대신 409 안내 |
| 8 | 구독 취소 | 테스트 결제(데모)는 화면 안내대로 즉시 종료 유지. 실결제 환경에서는 자동 갱신만 끄고 결제 기간 끝까지 유지 |
| 9 | 프록시 뒤 요청 제한 | 운영 모드에서 `TRUST_PROXY_HOPS` 미설정 시 부팅 경고. `.env.example`에 설명 추가 |
| 10 | 접근성 | 회차 버튼 `aria-label`(잠김·열림·현재 회차), 피드 탭·장르 칩 `aria-pressed`, 오류 화면 이미지 `alt`, 시놉시스 줄바꿈 유지 |
| 11 | 문구 정합성 | 원천징수율·무료 회차 수를 설정값으로 표시, "30일 이용권" → "숏핑 패스", 정산 안내 문구 현행화, 운영 기록의 액션 문구 20여 종 구분, 재생 화면 "자동 재생" 안내를 실제 조건(마지막 화·자동 재생 꺼짐·잠김)에 맞게 표시 |
| 12 | 회차 대기열 중복 번호 | 같은 번호가 있으면 경고 표시 + 올리기 버튼 비활성 |

### 함께 고친 소소한 항목
- 요금·정산 정책 저장 시 이 화면의 항목만 전송(다른 탭 설정이 되돌아가던 문제)
- 방송국 검색 결과가 없을 때 "아직 공개된 방송국이 없어요" 대신 "검색 결과가 없어요"
- AI 컷 화자는 같은 프로젝트 인물만 허용, 합성 자막의 화자 이름 정리
- 테스트 1건(`studio.test.mjs`)을 새 정책에 맞게 갱신: PD에겐 정리된 문구, 관리자에겐 원문

### 3차 작업 (대표님 지시 반영)

| 항목 | 조치 |
| --- | --- |
| 관리자 오류 표시 | 관리자 AI 작업 목록·시험 실행에서 **PD에게 보인 정리 문구와 공급사 원문을 함께** 표시(`error` + `error_detail`). PD 쪽 응답에는 원문 칸 자체가 없음 |
| 미사용 파일 보관 기간 | 최고 관리자 메뉴 **설정 · 기록 → 저장 공간 정리** 신설. 보관 기간 기본 **91일**, 관리자가 바꾸면 그 기간이 바로 적용(0 = 자동 정리 끔, 켤 때는 7~3650일). 정리 대상 미리보기(개수·용량·목록 20개), 지금 정리하기, 마지막 정리 기록, 운영 기록에 남김 |
| 개인 AI 한도 0 | **0 = 그 기간 사용 금지**로 변경. 비워 두면 하루 한도는 공통값, 월 한도는 제한 없음. 관리자 화면 안내 문구도 같게 수정 |
| AI 결과 다운로드 | 받으면서 크기를 세어 400MB를 넘으면 즉시 중단(서버 메모리 보호). 리다이렉트는 최대 5번, 매번 주소 재검사, 내부망·루프백 주소 차단, 다른 곳으로 넘어가면 공급사 키를 보내지 않음. 공급사 응답도 크기 한도 적용 |
| 회차 합성 중복 | 합성 버튼을 동시에 눌러도 한 번만 실행(검사 전에 자리 선점). 합성 중 프로젝트 삭제 차단. 합성 결과 등록 실패 시 만든 영상·자막 파일 삭제 |
| 결과 소유자 | 관리자가 PD 프로젝트·작품에서 AI를 대신 실행해도 결과 파일과 버전 기록의 주인은 그 PD |

### 검증
- 자동 테스트 **57/57 통과**(기존 50 + 신규 7: 한도 0, 결과 소유자, 동시 합성, 저장 공간 정리, 다운로드 리다이렉트, 금칙어, 오류 문구 정리). 두 번 연속 실행해 모두 통과
- 통합 시뮬레이션 **26/26 통과** (`npm run simulate`로 언제든 다시 실행 가능): PD 6명 동시 작업 16건·멱등키 중복·취소 겹침 후 지갑=장부 전수 대조, 예약 라마 0, 실패 작업 전액 반환, PD/관리자 오류 표시 차이, 보관 기간 91→30일 변경 시 삭제 범위, 모든 파일을 400일 전으로 바꿔도 사용 중인 파일 0개 삭제, 시청자 핑 충전·회차 열기·재생·정산 기록 회귀
- 관리자 화면(저장 공간 정리·AI 연결·운영 기록) 실제 브라우저로 열어 오류 없음 확인
- 타입 검사·빌드 통과

## 3. 확인했지만 문제 없던 영역

- API 경로·메서드·바디: 화면의 모든 호출이 서버 zod 스키마와 일치
- 인증·CSRF: httpOnly 쿠키 + Origin 검사 + `sameSite=lax`, 세션 토큰 해시 저장
- 관리자 전용 라우트 권한, 업로드 경로 탐색(모두 `basename`/정규식), ffmpeg 명령 주입(인자 배열)
- 핑 차감·정산 기록·출금 신청의 트랜잭션·멱등키·동시성 검사
- XSS(`dangerouslySetInnerHTML` 없음), useEffect 무한 루프 없음, 훅 순서 정상

## 4. 배포 전 체크리스트

- [ ] 서버 재시작(서버 코드 변경분 반영)
- [ ] `.env`에 `TRUST_PROXY_HOPS` 설정(터널/로드밸런서 뒤일 때)
- [ ] PostgreSQL로 `npm test` 한 번 실행(`DATABASE_URL` 지정)
- [x] "추가 권고" 12건 반영 완료
- [x] 3차 작업(관리자 오류 원문, 보관 기간 설정, 한도 0, 다운로드·합성·소유자) 반영 완료
- [ ] 저장 공간 정리 화면에서 보관 기간 확인(기본 91일)
